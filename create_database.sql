CREATE TABLE sucks (
    id integer primary key,
    msg text,
    user text,
    channel text,
    domain text,
    date datetime
);

CREATE TABLE rules (
    id integer primary key,
    msg text,
    user text,
    channel text,
    domain text,
    date datetime
);
