CREATE TABLE sucks (
    id integer primary key,
    msg text,
    user text,
    channel text,
    domain text,
    date integer
);

CREATE TABLE rules (
    id integer primary key,
    msg text,
    user text,
    channel text,
    domain text,
    date integer
);
